import { createTheme } from '@mui/material/styles'

export const DEFAULT = createTheme({
  palette: {
    primary: {
      main: '#3577D2',
    },
    secondary: {
      main: '#689f38',
    },
  },
})
