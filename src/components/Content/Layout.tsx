import * as React from 'react'
import Box from '@mui/material/Box'

export default function Layout() {
  return (
    <Box sx={{ height: '100%', display: 'flex', flexDirection: 'column' }}>Layout</Box>
  )
}
